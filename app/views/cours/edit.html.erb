    <div class="myflex content-title">
      <p><%= render 'shared/logo' %> /
        <%= link_to @student.fullname, @student %> / Modifier pré-inscription</p>
      <%= render 'shared/logout' %>
    </div>

    <div class="content-form">
      <%= render 'form', cour: @cour %>
    </div>

