<%= simple_form_for(@year) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name, label: "Saison 20??/20??" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
