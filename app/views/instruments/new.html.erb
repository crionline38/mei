<div class="row back rowshadow">
  <%= render 'shared/marge' %>
  <div class="content col s12 m9">
    <div class="myflex content-title">
      <p><%= link_to fa_icon('home'), adherent_path(@adherent) %> / <%= link_to "Instruments", instruments_path %> / Nouveau</p>
      <h4></h4>
    </div>
    <div class="content-form">
      <%= render 'form', instrument: @instrument %>
    </div>
  </div>
</div>
